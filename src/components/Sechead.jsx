import React from 'react';

function Sechead({ text, style, }) {
  return (
    <div className='w-[100%] flex'>
      {/* <a
        className={`bg-dark-primary px-4 text-center lg:mx-auto py-1 rounded-sm my-4 ${style}`}
      >
        {text}
      </a> */}
    </div>
  );
}

export default Sechead;
